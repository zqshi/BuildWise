# v0.33.0 Current — 待立项（占位）

> 状态: 占位（待规划）
> 起始 commit: （v0.32.0 归档后填）
> 上一版本: [v0.32.0](v0.32.0-snapshot.md)

## 版本目标

待规划。v0.32.0 投产前卫生收口完成（后端 lint 0 warnings + DEPLOY.md），BuildWise 已满足投产硬标准。候选后续工作（待立项评估优先级）：

- **前端 181 biome warnings 清理**（v0.32.0 遗留）：React hook 依赖项语义问题（`useExhaustiveDependencies`/`noNonNullAssertion`），复杂度高，非纯类型替换。
- **synthesizeTestMatrixOp 加 repair 循环**（v0.31.0 遗留）：LLM 过载返回非 JSON 时测试矩阵空降级，仿 `synthesizeReleaseReviewOp` 加 repair 兜底，提升按端测试用例产出稳定性。
- **DEPLOY.md §7 异步 full-cycle job 改造**：同步分析分钟级可能触发 nginx 504（后端继续跑），前端改用 full-cycle 异步 job 轮询，避免同步等待。

## 版本范围

待立项。立项时补充验证标准、任务依赖图、各任务实施要点。

## 任务依赖图

待立项。立项时补充依赖图。

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| - | 待立项（候选见版本目标） | - | - | pending | - |

状态值: pending / in-progress / blocked / done

## 对后续版本的约束

- 继承 v0.31.0/v0.32.0：lint warnings 清理后新增代码不得引入 `!`/隐式 any；DEPLOY.md 与配置同步维护。
- 实跑验证暴露的真实 LLM 行为问题若需 prompt 加固，记入对应版本约束。
