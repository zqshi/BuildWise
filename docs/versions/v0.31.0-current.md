# v0.31.0 Current — 待规划（v0.30.0 已归档）

> 状态: 待规划（无活跃任务）
> 起始 commit: 待定
> 上一版本: [v0.30.0](v0.30.0-snapshot.md)

## 版本目标

v0.30.0（按端质量数据 + LLM 按端评审）已归档，三验证标准全达成 + verify:all 全绿。本版本为占位 current：下一版本目标待规划，从 v0.30.0 遗留项选取。**禁止在任务进入本表前直接编码**（CLAUDE.md 任务编排规则）。

## 版本验证标准

待规划。

## 版本范围

**做什么:**
- 待规划

**不做什么:**
- 待规划

---

## 任务依赖图

待规划。

## 任务详情

| ID | 任务 | P | 依赖 | 状态 |
|----|------|---|------|------|

状态值: pending / in-progress / blocked / done

## 候选遗留项（待立项评估，非已激活任务）

- 前端按端展示端到端实跑验证（perPlatform 数据已流入 releaseReview，前端 v0.29.0 T6.7 UI 已就绪，留真实环境验证）
- 聚合根+hook 内聚超限文件（analysisService439/useProjectModelView406/useIterationWorkspaceState372/AnalysisDrawerPanels532/DashboardView448）接受不强拆
- 后端 biome warnings 清扫（noNonNullAssertion 历史债，散布 fullCycleOps/ontologyCodeRewriteBridge/sqliteRevokedTokenStore 等）
- dryRun 路径A 完整业务链路实跑（转后续手动，环境就绪需手动造数据+临时仓库+10min）

## 接续点（新会话起步）

### 必读
- [v0.30.0-snapshot.md](v0.30.0-snapshot.md)（按端质量数据 + LLM 按端评审已归档）
- `cd v2 && npm run check:boundaries && npm run check:version-discipline` 巡检（<10s）
- [backlog.md](backlog.md)（后续版本规划池 + 跨版本约束）

### 下一步
确认下一版本目标 → 立项（填任务依赖表）→ 按 CLAUDE.md DDD/TDD/文件治理执行。候选遗留项见上，从 backlog.md 长期技术债务表选取。

## 对后续版本的约束

- 继承 v0.30.0：按端质量数据为 LLM 按端评审必要前置，perPlatform 须有按端真实依据，编造防控（finalizeReleaseReviewPerPlatform）两条链须对齐不得绕过；
- TargetPlatform 固定枚举不变，新增端加枚举值。
