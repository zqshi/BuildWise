# v0.32.0 Current — 投产前卫生收口（lint warnings 清理 + 部署 runbook）

> 状态: 进行中
> 起始 commit: （v0.31.0 归档后填）
> 上一版本: [v0.31.0](v0.31.0-snapshot.md)

## 版本目标

v0.31.0 实跑验证 v0.30.0 按端展示端到端有效，verify:all 全绿可投产。但投产评估暴露 2 项历史债：后端 12 处 biome warnings（`noNonNullAssertion` 非空断言 + 隐式 any，CLAUDE.md 明确禁止 `!` 与 `any` 逃逸）+ 缺独立 DEPLOY.md 部署 runbook（docker-compose 未显式注入 AUTH_MODE/JWT_SECRET/CORS_ORIGINS，依赖 .env 派生但无文档说明）。本版本收口：lint warnings 用类型守卫替代 `!`（非功能变更）+ 起草 DEPLOY.md。

性质：技术债清扫 + 文档补全，非新功能。

## 版本验证标准

1. 后端 biome lint 0 warnings（12 处 `!`/隐式 any 全清，类型守卫替代，tsc + 670 测试无回归）；
2. DEPLOY.md 覆盖：生产 .env 派生（基于 `.env.production.example`）、AUTH_MODE/JWT_SECRET/CORS_ORIGINS 注入要求、服务编排（docker-compose）、健康检查、回滚/备份 ops 脚本、nginx `proxy_read_timeout` 与同步分析超时关注点；
3. verify:all 全绿（无回归）。

## 版本范围

**做什么:**
- T1 后端 12 处 lint warnings 清理（类型守卫替代 `!` 非空断言 + `let result` 隐式 any 显式类型）
- T2 起草 DEPLOY.md 部署 runbook

**不做什么:**
- 前端 181 warnings（React hook 依赖项语义问题，复杂度高，转后续）→ 不做
- per-prefix 超限文件拆分（backlog 已记接受不强拆）→ 不做

---

## 任务依赖图

```
T1 (lint 清理) ──→ T3 (归档质检)
T2 (DEPLOY.md) ──→ T3
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| T1 | 后端 12 处 lint warnings 清理（类型守卫替代 `!`） | P0 | - | pending | `conversationRequirementExtractor.ts`、`scheduledScanOps.ts`、`stageGateEvaluator.ts`、`iterationCoreOps.ts`、`ontologyCodeRewriteBridge.ts`、`fullCycleService.ts`、`fullCycleOps.ts`、`sqliteWorkspaceBacklog.ts`、`sqliteWorkspaceKnowledge.ts`、`sqliteRevokedTokenStore.ts` |
| T2 | 起草 DEPLOY.md 部署 runbook | P0 | - | pending | `docs/DEPLOY.md`（新建） |
| T3 | 归档质检 + verify:all | P0 | T1,T2 | blocked | 本文件 + git |

状态值: pending / in-progress / blocked / done

## 各任务实施要点

### T1 — 后端 12 处 lint warnings 清理

12 处分布（v0.31.0 归档评估时已读源码确认）：

1. `conversationRequirementExtractor.ts:173` `let result;` 隐式 any → 标注 `runWithContinuation` 返回类型
2. `scheduledScanOps.ts:41` `stages[i]!` → 已有 `i <= activeIdx` 守卫，用类型守卫
3. `stageGateEvaluator.ts:231` `artifactStageOrder[index+1]!` → 已有 index 边界守卫，用类型守卫
4. `iterationCoreOps.ts:54` `normalizeProject(project).repository!` → `hasGitRequirementIntakeTarget` 已守卫，用类型守卫
5-6. `ontologyCodeRewriteBridge.ts:39,66` `codeMap[matchedIndex]!`/`codeMap[i]!` → 已有 matchedIndex>=0 / 循环边界守卫，用类型守卫
7-8. `fullCycleService.ts:108,111` `this.delegates.X!(...)` → 三元已保证非空，改 `?.`（等价安全）
9. `fullCycleOps.ts:106` `STEP_ORDER[i]!` → 循环边界守卫，用类型守卫
10-11. `sqliteWorkspaceBacklog.ts:45` / `sqliteWorkspaceKnowledge.ts:45` `findXxx(id)!` → 刚插入查回，用类型守卫（仿 v0.30.0 `sqliteWorkspaceAnalysisStore` 修法）
12. `sqliteRevokedTokenStore.ts:37` `row.expires_at!` → optional 字段，改 `(row.expires_at ?? 0) <= now` 或类型守卫

遵循 CLAUDE.md：类型守卫替代 `!`，TDD（既有 670 测试守护），tsc + lint 0 warnings。

### T2 — 起草 DEPLOY.md 部署 runbook

基于：`Dockerfile`（多阶段 + 非 root appuser）/ `docker-compose.yml`（三服务 + healthcheck + 资源限制）/ `.env.production.example`（生产默认）/ `runtimeConfig.ts`（守卫）/ `nginx.conf`（CSP/HSTS/限流/proxy_read_timeout 180s）/ 9 个 ops 脚本。

覆盖：
- 生产 .env 派生（基于 `.env.production.example`，替换 JWT_SECRET/CORS_ORIGINS/ANTHROPIC_*）
- 鉴权注入：AUTH_MODE=jwt + JWT_SECRET≥32 + CORS_ORIGINS 显式（runtimeConfig production 强制，误配 fail-fast）
- 服务编排：docker-compose up（backend/openhands/frontend + repo-init 卷权限）
- 健康检查：/health /ready /api/v1/status
- nginx `proxy_read_timeout 180s` vs 同步分析分钟级超时关注点（分析超 nginx 504 但后端继续跑，建议异步 job 或调 timeout）
- ops 脚本：ops:preflight / ops:llm-check / ops:alerts / ops:rollback / ops:backup-drill
- 回滚流程

## 对后续版本的约束

- lint warnings 清理后，新增代码不得引入 `!` 非空断言与隐式 any（CLAUDE.md 既约束，biome severity=warn 不阻断但应保持 0）；
- DEPLOY.md 与 docker-compose/.env.production.example 同步维护，配置项变更须更新。
