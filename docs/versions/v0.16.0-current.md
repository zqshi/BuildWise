# v0.16.0 Current — 体检待修收尾 + policyGate/双状态/changeImpact 集成

> 状态: 活跃（2026-06-26 激活，v0.15.0 归档后承接）
> 起始 commit: 57053bb (2026-06-26，v0.15.0 归档)
> 上一版本: [v0.15.0](v0.15.0-snapshot.md)

## 版本目标

承接 v0.15.0 体检待修遗留 4 项：T2b 停止按钮 UI（v0.15.0 取消能力核心已就绪，仅缺前端 UI 入口）+ 诊断第 5/6/7 项（policyGate 治理盲区 / changeControl 与 fullCycle checkpoint 双状态解耦 / FullCycle Job 重启恢复对齐 Analysis Job + changeImpact 集成）。主题待用户确认后细化。

## 版本验证标准

1. fullCycle 运行中前端有可见「停止」按钮，点击后任务于步骤边界取消（后端 DELETE+信号已就绪）
2. policyGate 门禁覆盖 fullCycle 路径，不再仅 coach 路径触发
3. fullCycle checkpoint 与 changeControl 双状态有互查机制，不再各自维护
4. verify:all 真绿（含 v0.15.0 新增 lint 门槛）

## 版本范围

**做什么（待用户确认主题后细化）:**
- T2b fullCycle 停止按钮 UI（v0.15.0 起手任务）：ChatContext/ChatActionDeps/runFullCycleWithPolling 暴露 jobId → 停止按钮经 Connector→ProjectsWorkspace→IterationWorkspacePanel→ChatPanelArticle drilling 渲染（已核实：原任务表所列"ProjectsWorkspace.tsx/ChatComposer.tsx"系笔误，实为 Connector/Panels，已补 useIterationActions/iterationWorkspacePanelTypes/IterationWorkspacePanel）
- T5 policyGate 治理盲区：fullCycle 路径接入 policy 门禁审查（当前仅 coach 路径触发）
- T6 changeControl 与 fullCycle checkpoint 双状态解耦：两套状态互查机制
- T7 FullCycle Job 重启恢复对齐 Analysis Job（标 failed+一键重试）+ changeImpact 集成进 fullCycle 步骤链

**不做什么:**
- 前端 Props Drilling 整体重构 → 独立技术债务版本
- JSON 持久化生产化 → 需先方案规划

## 任务依赖表

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|---------|
| T2b | fullCycle 停止按钮 UI 入口 | P2 | - | done | ChatContext.tsx/chatActions.ts(ChatActionDeps)/chatActionFullCycle.ts/useIterationActions.ts/iterationWorkspacePanelTypes.ts/ProjectsWorkspaceConnector.tsx/ProjectsWorkspace.tsx/IterationWorkspacePanel.tsx/IterationWorkspacePanels.tsx |
| T5 | policyGate 治理盲区（fullCycle 路径接入） | P2 | - | done | policyGateOps.ts/policyOps.ts(fullCycleOps.ts/fullCycleService.ts/collaborationTypes.ts) |
| T6 | changeControl 与 fullCycle checkpoint 双状态互查 | P3 | - | pending | fullCycleOps.ts/coreOps.ts |
| T7 | FullCycle Job 重启恢复对齐 Analysis Job + changeImpact 集成 | P3 | - | pending | fullCycleService.ts/queueOps.ts/changeImpactDetection.ts |

## 约束

- 保持 verify:all 真绿（v0.15.0 起含 lint 门槛 + 子进程契约 + in-process 契约 + prompt 动态扫描）
- 遵守迭代声明纪律（docs/versions + 任务依赖表 + 6 项质量检测协议）
- 不修改 OpenClaw 源码
- Agent 框架可切换（声明+运行时分离）
- 长耗时管道必异步 Job + 前端轮询（v0.14.0 范式）
- 内存态 Job 须可重启恢复（v0.14.0 约束）
- fullCycle 取消采用步骤边界检查范式（v0.15.0 约束，T7 重启恢复改动须保留 cancel 检查点）
- autoResolve 默认守澄清门禁（v0.15.0 约束，不得回退默认旁路）
- 诊断证据须逐条核实（v0.15.0 修正诊断两处事实错误，后续接续须实际读代码核实而非照搬诊断结论）
