# v0.28.0 Snapshot — 剩余技术债清扫（analysisService 拆分+biome-ignore 评估+前端超限+占位删除+dryRun 验证）

> 状态: 已归档（2026-06-29）
> 起始 commit: 3021e60 (2026-06-29)
> 上一版本: [v0.27.0](v0.27.0-snapshot.md)

## 版本目标
v0.27.0 归档后，立项前逐条核实 backlog 6 项遗留真伪，校正两处误判（analysisPipelineOps 实测不超限 / 占位 5 死代码 vs 5 有意接口），将真实待处理项清扫：后端 analysisService 实质超限拆分 + 5 处 biome-ignore 评估 + 前端实质超限文件拆分 + ProjectsWorkspace 死代码占位删除 + dryRun 路径A 端到端验证。无新功能，纯代码卫生与端到端验证收口。

## 交付内容摘要
- **T1 analysisService.ts 472→438 拆分**（done）：核实为聚合根 class（WorkspaceService:66 装配，12 公开方法占~167 行不可拆）。拆 2 块可独立纯逻辑：restoreFromDb→analysisServiceRestore.ts(57) + retry 查找→analysisServiceRetry.ts(59)。修复 dead-code 三元（listAnalysisJobs 必选，WorkspaceRepository extends AnalysisPipelineRepository:182）。剩余聚合根内聚不强拆（用户确认"拆纯逻辑+接受剩余"）。
- **T2 5 处 biome-ignore 评估保留**（done，评估型）：消除需 18 处 ReturnType<typeof> 替换 + 3 别名（预想 3-4 倍工作量）。biome-ignore 是 biome useImportType 对 ReturnType<typeof> 的已知误报，有清晰注释（合理 lint 例外非坏味道）。保留跳过大重构（用户确认"保留合理例外"）。
- **T3 ProjectsWorkspace 删 5 个 `_` 死代码占位**（done）：删 versionSnapshots/templateRuns/deployments/onCreateDeployment/onTransitionDeployment + 清理 unused import（DeploymentRecord/TemplateRunHistory/VersionSnapshot）。保留 5 个 model*/opsMetrics 有意接口。调用链唯一（MainContentArea→Connector→ProjectsWorkspace 零透传）零影响。
- **T4 前端超限文件拆分**（done）：useIterationWorkspaceState 446→369（拆 6 纯函数→iterationWorkspaceStateHelpers.ts 99）+ useProjectModelView 416→405（拆 1 纯函数→useProjectModelViewHelpers.ts 20）。AnalysisDrawerPanels 530/DashboardView 447 组件内聚接受（用户确认"拆2 hook+2组件接受"）。
- **T5 dryRun 路径A 评估转后续**（done，评估型）：环境就绪（后端运行:5055+claude CLI 2.1.177+LLM 配齐）但实跑需造数据+临时仓库+10min 手动端到端。路径B(v0.11.0)+fullCycle 实跑(v0.16.0 T2b)已验证核心范式。转后续手动不阻塞归档。

## 关键技术决策表
| 决策 | 选择 | 理由 |
|---|---|---|
| T1 聚合根 class 拆分 | 拆纯逻辑+接受内聚剩余 | 12 公开方法占167行不可拆（导出留原文件范式），拆 private 纯逻辑最多降到~419仍超软限，强拆破坏聚合根边界 |
| T1 dead-code 三元修复 | 去三元直接调用 | listAnalysisJobs 必选（WorkspaceRepository extends AnalysisPipelineRepository:182），原 this.repo.listAnalysisJobs? 检查必选方法属 dead code |
| T1 TS2774 根因 | class this vs 参数差异 | this.repo.method?（class）不报 TS2774 而 param.method?（参数）报，TS 对 class 上下文方法引用 condition 的边角差异 |
| T2 biome-ignore | 保留合理例外 | 18 处 ReturnType<typeof> 替换大重构价值/工作量比低，biome 误报有注释非坏味道 |
| T4 hook 拆分 | 拆纯逻辑+接受内聚 | hook 复合体类似聚合根，纯函数可拆但 hook 体内聚（useState/useEffect/useMemo 调用顺序依赖） |
| T5 dryRun | 转后续手动 | 验证型非编码，路径B+fullCycle 已验证范式，实跑需手动端到端 |

## 遗留问题
- **analysisService.ts 438 行**（超软限 118）：聚合根 service 公开接口+字段+编排薄包装内聚，强拆破坏聚合根边界，接受
- **useProjectModelView 405 行**（超软限 45）：纯函数密度低（仅 1 个），7 hook 复合体内聚，接受
- **useIterationWorkspaceState 369 行**（超软限 9）：hook 复合体内聚，接近软限，接受
- **AnalysisDrawerPanels 530 / DashboardView 447**：组件内聚（导出组件/单组件），零桥接范式拆辅助降有限，接受
- **5 处 biome-ignore**：biome useImportType 对 ReturnType<typeof> 误报，有注释合理例外，保留
- **T5 dryRun 路径A 实跑**：环境就绪但需手动造数据（迭代+代码仓库+boundaryCodePaths）+临时 git 仓库+10min 轮询，转后续手动（复现：dev:stack:start + curl 造数据 + node backend/scripts/dryrun-code-rewrite.mjs http://127.0.0.1:5055 <iterId> "把按钮文案改成提交"）

## 对后续版本的约束
- analysisService 拆分后核心分析服务结构定型，"拆纯逻辑+接受聚合根内聚"为后续超限治理范式
- biome-ignore 保留为合理例外，新代码 ReturnType<typeof> 用法沿用注释说明（非强行消除）
- 前端 hook 拆分范式：拆纯函数辅助，hook 体内聚不强拆（useState/useEffect/useMemo 调用顺序依赖）
- ProjectsWorkspace 占位收敛：5 个 model*/opsMetrics 保留备接入，5 个 `_` 死代码已删

## 质量检测结果（6 项，全过）
1. 死代码/孤立文件：T1+T4 拆出 4 文件全被原文件 import 无孤立；check:hygiene passed ✓
2. 依赖卫生：package.json 未改（T1-T4 refactor 未加依赖）✓
3. 配置一致性：runtimeConfig/.env.example 未改 ✓
4. 文档对齐：CLAUDE.md + CONTRIBUTING.md + backlog.md 存在 ✓
5. 架构合规：check:boundaries（硬限 1000）+ tsc 双端 0 + lint 0 ✓
6. 仓库卫生：.gitignore 完备无构建产物入库，T1-T4 代码分层入库（b2dcfcb 后端 application + 6082671 前端）✓

verify:all：`cd v2 && npm run verify:all` 全绿（前端 230 fail 0[228pass/2skipped] + 后端 603 fail 0 + 契约 passed + prod-release alerts 空）。
