# v0.13.0 Current — 需求池→版本归属交互优化

> 状态: 活跃（T1.x / T5 / T7 / T8 done；T6 待推进，2026-06-26）
> 起始 commit: 待定（首个 T1 提交时填）
> 上一版本: [v0.12.0](v0.12.0-snapshot.md)

## 版本目标

v0.12.0 已恢复 verify:prod-release 生产门禁并修复 4 个真实 bug。本版本聚焦用户反馈的产品交互痛点：**需求池完成版本归属后，需求仍堆在需求池、归属后无"移入版本"视觉闭环，多版本时不便管理查看**。

诊断确认后端能力齐全（`BacklogItem.iterationId` + `assignBacklogItemsToIterationOp` + `assignBacklogItems` API 全支持），问题在前端 `BacklogPanel` 默认视图为平铺 list、归属仅有下拉。本版本：默认视图改版本分组 + HTML5 原生拖拽归属（复用 `IterationWorkspacePanels` 既有 DnD 模式，零新依赖），**后端零改动**。

## 任务依赖表

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|---------|
| T1 | 需求池→版本归属交互优化（默认版本分组+拖拽归属） | P0 | - | done | BacklogPanel.tsx / backlogGrouping.ts / css |
| T1.1 | 抽 groupBacklogByIteration 纯函数 + 测试 | P0 | T1 | done | src/domain/workspace/backlogGrouping.ts / tests/backlogGrouping.test.ts |
| T1.2 | 默认视图改版本分组 | P0 | T1.1 | done | BacklogPanel.tsx |
| T1.3 | 拖拽归属 + CSS | P0 | T1.2 | done | BacklogPanel.tsx / workspace-backlog-knowledge.css |
| T1.4 | 下拉保留（可访问性兜底）+ 版本角标 | P1 | T1.3 | done | BacklogPanel.tsx |
| T1.5 | 验证 typecheck/test/verify:all + 浏览器 | P0 | T1.4 | done（门禁全绿+API数据链路双向验证通过，浏览器视觉待用户） | - |
| T1.6 | 修复取消归属 status 不回退缺陷 | P0 | T1.5 | done | backlogOps.ts / backlog-assign-status.test.mjs |
| T2 | 路径A 完整 dryRun 脚本（v0.12.0 deferred 迁入） | P2 | - | blocked | dryrun-code-rewrite.mjs |
| T4 | 技术债务深化（Props Drilling / JSON 持久化，v0.12.0 deferred 迁入） | P3 | - | blocked | v2/src/pages/projects/ |
| T5 | 修复契约 harness 路径双模式 bug + 子进程模式 seed 缺失 | P0 | - | done | tests/contractHarness.mjs(versionedPath复用+ALLOW_SEED) / httpTestClient.mjs(export) / contract-harness-path.test.mjs |
| T6 | verify:prod-readiness 接入契约测试（修 glob 跳过 contract.mjs） | P0 | T5,T8 | pending | backend/package.json |
| T7 | Prompt 检测器增扫 src/ 动态 prompt + 修 5 处 userPrompt schema 泄露违规 | P0 | - | done | check-prompt-quality.mjs(增 scanUserPromptSchemaLeak) / uxGuidanceOps / visualEditOps / codeRewriteOps / coreAnalysisAgentOps / qualityAuditAgentOps / prompt-quality-scanner.test.mjs |
| T8 | 修复契约 git-intake 子进程模式 waitForHealth 无轮询致 ECONNREFUSED | P0 | - | done | tests/httpTestClient.mjs(waitForHealth 加轮询) |

状态值: pending / in-progress / blocked / deferred / done
依赖列: "-" 表示无依赖

> T2/T4 从 v0.12.0 deferred 迁入，本版本可后续激活；当前优先 T1。

## 关键路径

```
T1.1（纯函数+测试）→ T1.2（默认视图）→ T1.3（拖拽）→ T1.4（兜底+角标）→ T1.5（验证）
```

## 约束

- 保持 verify:all 全绿（含 production-release 契约验证，v0.12.0 已恢复）
- 遵守迭代声明纪律（docs/versions + 任务依赖表 + 质量检测协议）
- 不修改 OpenClaw 源码
- Agent 框架可切换（声明+运行时分离）
- T1 业务后端零改动（归属能力已齐全）；T5/T6/T7 为质量门禁加固例外（仅动测试与检测脚本，不动业务逻辑）
- 拖拽用 HTML5 原生 DnD 复用既有先例，不引入 dnd-kit
- T5/T6/T7（门禁失效修复）是 v0.13.0 归档前置：契约与 prompt 两道门禁须真正生效，verify:all 方算真绿
