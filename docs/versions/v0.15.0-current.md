# v0.15.0 Current — 体检待修修复

> 状态: 活跃（2026-06-26 激活，主题由"待规划"确定为体检待修修复）
> 起始 commit: 42aef6a
> 上一版本: [v0.14.0](v0.14.0-snapshot.md)

## 版本目标

修复 6/26 质量体检发现的 4 项代码层待修点（P1×2 + P2×2），verify:all 真绿（含新增 lint 门槛）。诊断第 5/6/7 项（policyGate 治理盲区 / changeControl 与 fullCycle checkpoint 双状态解耦 / FullCycle Job 重启恢复对齐 Analysis Job + changeImpact 集成）遗留 v0.16.0。

核实证据时修正诊断两处事实：
1. autoResolve 旁路：非"后端默认旁路"，而是前端 `chatActionFullCycle.ts:82,103` 硬编码 `autoResolveClarifications: true` + 后端 `fullCycleSteps.ts:65` `!== false` 双保险；confirmation 步骤已有 blocked 分支(81-85)只是被旁路。
2. lint 漏网：诊断只报前端 7 error，实测后端还有 36 error（共 43 error）；前端 error 多非 FIXABLE，后端 error 多 FIXABLE。

## 版本范围

- **T1 merge-rewrite 空转修复**：`FullCycleStepResults` 加 `rewriteRuns` 暂存两次改写结果，`merge-rewrite` 用它替代字面 `[]`
- **T2 fullCycle 中断/取消**：DELETE 路由 + 步骤边界 cancel 标志（方案 A，不粗暴中断 LLM）+ 前端停止按钮
- **T3 autoResolve 默认守门禁**：默认改 `=== true`，移除前端硬编码 true，confirmation 已有 blocked 分支自然生效
- **T4a 清前后端 43 lint error**：后端 biome --apply 为主 + 前端手动修
- **T4b lint 纳入 verify:all**：依赖 T4a

## 任务依赖表

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|---------|
| T1 | merge-rewrite 空转修复 | P1 | - | pending | fullCycleSteps.ts, fullCycleOps.ts |
| T2 | fullCycle 中断/取消能力 | P1 | - | done | fullCycleJobOps.ts, fullCycleOps.ts, fullCycleService.ts, workspaceIterationAnalysisRoutes.ts, 前端 fullCycleJobPoll.ts/workspaceApiAgentOps.ts/chatActionFullCycle.ts |
| T2b | 停止按钮 UI 入口 | P2 | - | pending | ChatContext/ChatActionDeps/chatActionFullCycle/新停止按钮组件/ProjectsWorkspace（多层 props drilling 改造，遗留 v0.16.0 与前端 UI 重构合并；T2 取消能力核心已完成：后端 DELETE+信号+cancelled 509 测试绿，前端 API+轮询+catch 闭环） |
| T3 | autoResolve 默认守澄清门禁 | P2 | - | pending | fullCycleSteps.ts, chatActionFullCycle.ts |
| T4a | 清前后端 43 个 lint error | P2 | - | pending | 前后端若干 lint 文件 |
| T4b | lint 纳入 verify:all | P2 | T4a | done | v2/package.json, v2/backend/package.json |

实现顺序：T1→T3（同改 fullCycleSteps，合并一批提交）→ T2 → T4a → T4b。

## 约束

- 保持 verify:all 真绿（v0.13.0 后含子进程契约 + in-process 契约 + prompt 动态扫描；本版本新增 lint 门槛）
- 遵守迭代声明纪律（docs/versions + 任务依赖表 + 6 项质量检测协议）
- 不修改 OpenClaw 源码
- Agent 框架可切换（声明+运行时分离）
- 长耗时管道必异步 Job + 前端轮询（v0.14.0 确立范式）
- 内存态 Job 须可重启恢复（v0.14.0 确立约束）
- 中断采用步骤边界检查（方案 A），不粗暴 abort LLM 调用，与 checkpoint 续跑契合
