# v0.26.0 Snapshot — 遗留项统一收口（本体链+多租户+前端测试债）

> 状态: 已归档（2026-06-29）
> 起始 commit: 9939c0c (2026-06-29)
> 上一版本: [v0.25.0](v0.25.0-snapshot.md)

## 版本目标
v0.25.0 完成本体评审解决流程后，将跨版本积累的技术遗留统一收口：v0.25.0 发现的本体链 3 项、v0.23.0 多租户遗留、v0.21.0 前端测试债。按 P0/P1 分批，夯实既有功能的质量与安全底座，无新功能。真实投产要求下核实风险、修复真 bug、不为不存在的风险强加 scope。

## 交付内容摘要
- **T1 候选快照版本化**（done，方案 B）：`nextCandidateVersionNumber` 纯函数 + `planIterationModeling` 接入，candidate id 含版本序号 `snapshot-X-Y-v${n}-candidate`，publish 后再 saveCandidate 生成新版本不覆盖 published（核心 bug 修复），旧 id 无序号视为 v0 向后兼容。**REST 补漏**：抽 `resolvePublishFailureHttp` 纯函数，补 v0.25.0 T2 遗漏的 `unresolved_blocking_reviews`→409+中文（原走 404 误报）。12 测试。
- **T2 mock/真实 repo 一致**（done）：`createInMemoryModelingRepo.saveCandidateSnapshot` push→upsert 对齐真实 repo。修复 mock 掩盖的真实行为 bug——resolve 写回同 id 时第二次 resolve 读旧快照致 resolved 未累积、publish 误阻断。3 测试。
- **T3 onAnalysisConfirmed 集成测试**（done，纯测试）：补 app.ts:209-225 装配链盲区，3 例覆盖 confirmIterationAnalysis→回调→saveCandidate→自动 publish→门禁阻断停 candidate 全链。无新代码，门禁逻辑 T1/T2 已实现。本体链 T1-T3 整段收口。
- **T4 typed table tenant scope**（done，已核实无风险不改代码）：listIterations/findIteration/listMessages 入口经 ensureIterationAccess/ensureProjectAccess 校验；listAuditLogs 是平台超管全局审计视图（全局路由+requireAdmin+无 tenant_id 列）。无跨租户串风险，不为不存在的风险强加 DB scope。
- **T5 assistant_messages tenant_id**（done，已生效无风险不改代码）：schema 008 有列+索引；DB 层 list/append/clear 三者 SQL 均 WHERE tenant_id=?；route 入口 resolveAuthTenantId 从认证上下文取。全链生效。
- **T6 useAuthController hook 副作用单测**（done，v0.21.0 T3 遗留收口）：引入 @testing-library/react v13 + renderHook，3 例覆盖 tenant-stale→refreshSession / auth-expired→resetAuthState / 无 prior session 不调 refreshSession。

## 关键技术决策表
| 决策 | 选择 | 理由 |
|---|---|---|
| T1 candidate id 策略 | 方案 B（版本序号） | 允许多版本候选并存不覆盖 published，不限重做本体建模；方案A 禁止 publish 后再 save 限制过强 |
| T1 旧数据兼容 | 旧 id 无序号视为 v0 | 新候选从 v1 起，旧 published 不被覆盖 |
| T1 REST 映射 | 抽 resolvePublishFailureHttp 纯函数 | unresolved_blocking_reviews→409（快照存在但不满足发布条件），非 404 |
| T2 mock 一致 | push→upsert | 对齐真实 repo，消除 resolve 写回累积 resolved 被 mock 掩盖的 bug |
| T4/T5 风险处置 | 已核实无风险不改代码 | 实证入口均经 tenantAccess 校验/全链已用 tenant_id，不为不存在的风险强加 scope（强加反增维护面） |
| T6 hook 测试 | renderHook + jsdom Event 注入 | hook 须组件树内调（直接调 Invalid hook call）；jsdom Event/CustomEvent 须注入 global（dispatchEvent 校验事件归属） |

## 遗留问题
- **A→B 回写冗余未评估**（v0.24.0 遗留）：publishSnapshot 的 snapshotToKbPatch 把快照内容回写 KB（A 套快照与 B 套 KB 双写绕圈），是否收敛为 KB 视图层未定。→ v0.27.0 T1
- **per-prefix 超限文件**（15 个，321-531 行，<800 硬限）：既有文件非本版引入，软限 warning 非阻塞。→ v0.27.0 T2
- **前端 Props Drilling 残留评估**（v0.17.0 已迁 7 Context）：是否还有残留需收敛未评估。→ v0.27.0 T3

## 对后续版本的约束
- 本体候选快照版本化后，publish 不再覆盖历史 published；多版本候选可并存可追溯
- mock/真实 repo 语义一致后，集成测试可用 mock 不再掩盖真实行为 bug
- typed table tenant scope 经核实无风险，新增关系型 typed table 查询默认支持可选 tenantId（schema 纪律延续 v0.23.0）
- 前端 hook 副作用测试基础设施就绪（@testing-library/react + jsdom Event 注入），后续 hook 副作用可复用

## 质量检测结果（6 项，全过）
1. 死代码/孤立文件：check:hygiene passed ✓
2. 依赖卫生：check:hygiene passed ✓
3. 配置一致性：未改 runtimeConfig/.env.example（仅加 @testing-library/react devDep）✓
4. 文档对齐：CLAUDE.md + backlog.md + v0.26.0 文档存在 ✓
5. 架构合规：check:boundaries passed（软限 warning 非阻塞）+ tsc 双端 0 + 新增文件均 <800/函数<60 ✓
6. 仓库卫生：工作树干净，代码分层入库 ✓

verify:all：`cd v2 && npm run verify:all` 全绿（前端 230 fail 0 + 后端 601 fail 0 + 契约 Contract/git-intake passed + verify:prod-release preflight ok/alerts 空）。
