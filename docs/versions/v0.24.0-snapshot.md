# v0.24.0 Snapshot — 突出核心价值，主线增量夯实（A套元能力门禁激活）

> 状态: 已归档（2026-06-28）
> 起始 commit: edf28ab (2026-06-28)
> 上一版本: v0.22.0（已归档）；v0.23.0 暂停让位（见 backlog）

## 版本目标
BuildWise 核心价值 = 活的知识链条（本体）。经动态调用链核查（含运行时兜底 + 现有测试核实，非静态 grep）确认：B 套 ProjectKnowledgeBase 真活（变更影响/门禁/代码回流消费），A 套 ModelSnapshot 快照系统的两类元能力空转——状态机门禁空转（门禁不读 latestSnapshotStatus）、评审门禁空转（门禁不检查 ReviewTask blocking）。本版激活两类空转元能力为"本体发布门禁"，突出核心价值。TDD 先 Red 后 Green。

## 交付内容摘要
- **T1 元能力门禁 Red 基线**（done）：`tests/ontology-release-gate.test.mjs`，刻画 evaluateOntologyReleaseGate 期望行为。
- **T2a evaluateOntologyReleaseGate 纯函数**（done）：`domain/continuousModeling/ontologyReleaseGate.ts`，温和策略（无快照放行/有快照须published/发布即认可）。6 单测 Green。
- **T2b 接入发布门禁**（done）：`application/continuousModeling/ontologyReleaseGateService.ts`（新，桥接 workspace+continuousModeling，与 ChangeImpactService 同构）+ `fullCycleOps` delivery-package 步骤 delegate 接入（detectChangeImpact 先例）+ `fullCycleService` 传递 + `workspaceService` 注入。5 单测 Green。
- **T2b 投产风险修正**（挖掘改造）：核查发现 ReviewTask 零解决路径 + onAnalysisConfirmed 自动 publish，原"published+blocking 阻断"会误卡正常发布流程。校准语义：published=发布即认可（不查历史 blocking 标记），评审阻断只在 candidate（未发布）阶段作补充理由。

## 关键技术决策表
| 决策 | 选择 | 理由 |
|---|---|---|
| 元能力处置 | 全激活（状态机+评审门禁）而非砍冗余 | 突出核心价值，把空转元能力变差异化能力 |
| 接入方式 | delegate 注入（detectChangeImpact 先例）不改持久化 | 投产安全，避免改 IterationChangeControl 类型波及持久化/前端 |
| 门禁严格度 | 温和（无快照放行） | 不强制每个迭代做本体，不误阻断现有流程 |
| 评审门禁语义 | 发布即认可（published 不查历史 blocking） | ReviewTask 无解决路径，published+阻断会卡死正常发布；published=用户已确认分析(含评审) |
| 继承链 T2c | 去除（伪需求） | continuousModelingService:23 兜底读 published baseline + 有测试，继承链已工作 |
| 测试基线 | 补 Red 非重建 | 核查纠正"零测试"误判，本体链路已有 7 个 .mjs 测试覆盖 |

## 遗留问题
- **评审门禁当前是"候选态阻断+发布即认可"语义**：真正独立评审解决流程（用户确认术语/规则后清除 blocking 评审，使 candidate→publish 前须解决）留后续增量版本。当前评审阻断仅作用于"未发布且有 blocking"的候选快照。
- **A→B 回写冗余未处理**：publishSnapshot 的 snapshotToKbPatch 把快照内容回写 KB（绕圈），本版不动（不属激活范围），留后续评估是否收敛为 KB 视图层。
- check:boundaries 软限 warning（artifactOps 338/coreOps 431/ontologyService 455 等既有文件），非本版引入，硬限 800 enforced。

## 对后续版本的约束
- 本体发布门禁激活后，delivery-package 前检查快照状态：温和（无快照放行/有快照须 published），评审解决流程就绪前不独立阻断 published 快照
- 元能力不再完全空转（状态机被门禁消费；评审作候选态阻断）
- v0.23.0 tenant 硬隔离恢复时机：主线夯实后（核心 bug projects.tenant_id 不写入仍在，恢复时优先修）

## 质量检测结果（6 项，全过）
1. 死代码/孤立文件：check:hygiene passed ✓（verify:all 含）
2. 依赖卫生：check:hygiene passed ✓
3. 配置一致性：未改 runtimeConfig/.env.example ✓
4. 文档对齐：CLAUDE.md + backlog.md + v0.24.0 文档存在 ✓
5. 架构合规：check:boundaries passed（软限 warning 非阻塞）+ tsc 双端 0 + 新文件均 <800（ontologyReleaseGate 36行/OntologyGateService 33行）✓
6. 仓库卫生：工作树改动待分层 commit ✓

verify:all：`cd v2 && npm run verify:all` 全绿（前端 227 + 后端 560[含本体门禁 11 新测试] + 契约 Contract/git-intake passed + verify:prod-release preflight ok/alerts 空/contract passed）。

## 核查方法教训（写入供后续会话）
本轮反复出现核查偏差（A套定性、releaseReviewOps读本体、零测试、继承链空转），根因是静态 grep 看不出运行时兜底 + 没读全 service 层和现有测试就下结论。已每次按实证修正。后续：先读全运行时逻辑（含兜底分支）+ 现有测试再断言，不靠静态 grep 关键词。
