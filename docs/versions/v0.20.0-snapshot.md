# v0.20.0 Snapshot — 规范漂移校正 + 代码清理

> 状态: 已归档（2026-06-28）
> 起始 commit: e2bae44 (2026-06-28)
> 上一版本: [v0.19.0](v0.19.0-snapshot.md)

## 版本目标
v0.19.0 归档遗留段与 backlog P0 列了「规范漂移校正」主题。立项前逐条核实发现遗留 4 条里 **2 条不实**（① check-version-discipline.mjs 实际存在于 `v2/scripts/`、已挂 v2/package.json 并纳入 verify:all；④ in-memory mock repo 缺方法不存在——全仓无 mock repo、maybeExtractExperience 无测试、sqlite 有实现）。本版校正不实记录，补齐真正缺失配置（EXPERIENCE_SCAN 未入 .env.example），清理既有 lint 残留（stageOrchestrator:117），修正 CLAUDE.md 会话巡检命令路径（根目录无 package.json），并修复 T5 验证时发现的契约测试 LLM 穿透（v0.19.0 既有 bug）。本版无业务行为变更，属卫生校正版本。

## 交付内容摘要
- **T1 补 .env.example EXPERIENCE_SCAN 配置**（done）：app.ts:112-114 读取 BUILDWISE_EXPERIENCE_SCAN_ENABLED（默认关）+ INTERVAL_HOURS（默认 6），补入 `v2/backend/.env.example`。
- **T2 CLAUDE.md 巡检命令路径校正**（done）：根目录无 package.json，裸 `npm run` 跑不起来。改 `cd v2` 风格（巡检 `cd v2 && npm run check:boundaries && npm run check:version-discipline`；归档 `cd v2 && npm run verify:all`）。方案演进：初版用 `npm --prefix v2`（单命令可行），但 verify:all 内部含 `npm --prefix backend` 嵌套前缀，`--prefix v2 run verify:all` 解析出错（拼出 `v2/backend/v2/package.json`），故统一 `cd v2`。TEMPLATE.md:98 原本正确，未改。
- **T3 stageOrchestrator:117 noImplicitAnyLet 清理**（done）：`let continuationResult` → 显式 `ContinuationResult` 类型；ContinuationResult 在 `agentContinuation.ts:19` 声明未导出，改 `export type`（消费方引用返回类型本就该导出）。
- **T4 不实遗留记录校正**（done）：v0.19.0-snapshot.md 遗留段加核实块（第 1/4 条不实，第 2/3 条属实已修）；backlog.md P0 更新为 v0.20.0 已立项 + v0.21.0~v0.23.0 规划。
- **T6 契约测试 LLM 穿透修复**（done，T5 验证时发现）：`contractHarness.mjs` 的 `serverEnv={...process.env}` 继承父 shell 的 `ANTHROPIC_*`，`if(!llmConfigured)` 只置空 `LLM_*` 遗漏 `ANTHROPIC_*`，而 `agentRunnerConfig` provider=anthropic-compatible 优先读 `ANTHROPIC_*` → 走真实 glm-5.2 → TPM 限流使 verify:all 不稳定。修复：置空键集与 `src/index.ts` llmOverrideKeys 全集一致（补 `LLM_PROVIDER` + `ANTHROPIC_BASE_URL/AUTH_TOKEN/MODEL`）。

## 关键技术决策表
| 决策 | 选择 | 理由 |
|---|---|---|
| 巡检命令路径 | `cd v2 && npm run ...`（非 `npm --prefix v2`） | verify:all 内部含 `npm --prefix backend` 嵌套前缀，`--prefix v2` 解析出错；`cd v2` 与 TEMPLATE.md 一致 |
| ContinuationResult 导出 | `export type` | stageOrchestrator 需注解 continuationResult 类型，返回类型本就该导出供消费方引用 |
| 契约测试 LLM 穿透修复 | 置空键集对齐 llmOverrideKeys 全集 | 让 CONTRACT_ENABLE_LLM 未设时真正走 mock，verify:all 稳定不再受 glm-5.2 TPM 限流 |
| 不实遗留记录处理 | 加核实块（不删原文） | 保留归档历史可追溯，标注真伪防误导后续立项 |
| 版本范围切分 | 4 方向拆 v0.20.0~v0.23.0 依次推进 | 一版本一主题，避免架构重版本+卫生小版本混入单版本 |

## 遗留问题
- **lint 仍有 5 处同类 noImplicitAnyLet**：conversationRequirementExtractor:173 / authRoutes:136 / workspaceIterationAnalysisJobRoutes ×4，非本版范围（本版仅清 stageOrchestrator:117），留后续版本清理。
- **契约 verify-production-release.mjs 的 analysis 在 mock 下返回 503**：in-process prod-release verify 中 `POST /iterations/3/analysis statusCode=503`（无 LLM 时 analysis 不可用），属预期行为（llm configured=false），非回归。contract.mjs 的 lifecycle scenario 用独立 harness 走 mock analysis 返回 200 通过。
- 后续版本规划：v0.21.0 前端副作用单测 / v0.22.0 owner 收敛 / v0.23.0 多租户 DB 硬隔离。

## 对后续版本的约束
- **归档遗留记录必须核实真伪再写入**（教训：v0.19.0 遗留 4 条 2 条不实，误导 v0.20.0 立项前核实成本）
- 会话巡检命令须 `cd v2` 跑（根目录无 package.json；verify:all 含嵌套 `--prefix backend` 不可用 `--prefix v2`）
- 契约测试 LLM 穿透已修，CONTRACT_ENABLE_LLM 未设时走 mock，verify:all 稳定（不再依赖外部 glm-5.2 可用性）
- 一版本一主题，4 方向拆版本依次推进

## 质量检测结果（6 项，全过）
1. 死代码/孤立文件：check:hygiene passed ✓
2. 依赖卫生：check:hygiene passed ✓
3. 配置一致性：EXPERIENCE_SCAN 补 .env.example 与 app.ts 读取对齐 ✓；契约 LLM 穿透修复（置空键集对齐 llmOverrideKeys）✓
4. 文档对齐：CLAUDE.md 巡检命令路径校正 ✓
5. 架构合规：check:boundaries passed（15 警告非阻断，既有超 320 行文件非本版引入）+ tsc 双端 0 ✓
6. 仓库卫生：git status 干净（归档前分层提交），dist/.env 已 gitignore ✓

verify:all：`cd v2 && npm run verify:all` exit 0 全绿（548 单元测试 pass + Contract test passed + contract.git-intake.mjs passed + verify:prod-release: preflight/alerts/llm/backup 全 ok，llm configured=false 走 mock）。
