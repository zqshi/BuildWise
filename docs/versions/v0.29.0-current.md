# v0.29.0 Current — 目标端维度 MVP 闭环（堵死「虚假 go」）

> 状态: 进行中
> 起始 commit: 2b7d94a (2026-06-29)
> 上一版本: [v0.28.0](v0.28.0-snapshot.md)

## 版本目标

当前 BuildWise 的「分析→交付物→测试→发布」是单维度整体链路，domain 模型没有「目标端」轴。多端项目（web + iOS + Android + 鸿蒙）下，整体 `go` 掩盖单端短板——web 端测试撑高整体 passRate、agent 改 web 路径即「边界内完成」，原生端零覆盖被合法掩盖，迭代标记「可发布」但只有 web 端真实可投产（虚假推进）。

本版本引入「目标端」一等维度，让发布评审按端分别给 decision/blockers，整体可发布 = 各声明端均 `go`，在门禁层堵死虚假 go。能力边界：BuildWise 不替代各端 CI/构建，本期「真实投产可用」= 交付物归属端 + 该端测试通过 + 该端代码改动落地 + 门禁按端聚合，不接各端构建链（留后续）。

## 版本验证标准

1. 项目可声明目标端集合（`targetPlatforms`），默认 `["web"]` 向后兼容；
2. 发布评审对每个声明端分别给出 `decision/blockers`，顶层 `decision` 由规则聚合（有 block→block / 全 go→go / 否则 caution）；
3. publish 门禁：任一声明端缺失或非 `go` → 整体不可发布并返回阻断原因（虚假 go 被堵）。

## 版本范围

**做什么:**
- domain：TargetPlatform 枚举 + Project.targetPlatforms + 交付物 targetPlatform + releaseReview.perPlatform（顶层 decision 改聚合）
- application：releaseReview LLM 按端评审（governanceRunnerOps）+ 解析（releaseReviewOps）+ 聚合（qualityOps）+ finalize 透传 + publish 门禁「各端均 go」
- infrastructure：迁移 008 + Project 持久化读写 targetPlatforms
- interfaces：Project 创建/更新 schema 接受 + 校验 targetPlatforms
- 前端：项目设置端声明 + 发布评审按端展示
- 测试：新增 perPlatform 聚合 + 门禁单测，适配 ~15 个既有测试

**不做什么:**
- 测试矩阵 generatedTestMatrix 按端分组 coverage/passRate → v0.30.0
- 代码路径 codePathWhitelist/boundaryCodePaths 按端白名单 → v0.30.0
- 各端 CI/构建链接入（超出 BuildWise 边界）→ 按需

---

## 任务依赖图

```
T1 (domain 类型) ──→ T3 (releaseReview 按端+聚合) ──→ T4 (publish 门禁)
                                                     └──→ T7 (测试适配)
T1 ──→ T2 (迁移+持久化) ──→ T5 (interfaces schema)
T1 ──→ T6 (前端)
T3,T4,T5,T6 ──→ T7 ──→ T8 (归档质检)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| T1 | domain 类型：TargetPlatform + Project + artifact + releaseReview.perPlatform | P0 | - | pending | `domain/workspace/projectTypes.ts`、`analysisTypes.ts` |
| T2 | 迁移 008 + 持久化读写 targetPlatforms | P0 | T1 | pending | `infrastructure/persistence/migrations/008_*`、`sqliteWorkspace*.ts` |
| T3 | releaseReview LLM 按端评审 + 解析 + 聚合 | P0 | T1 | pending | `governanceRunnerOps.ts`、`releaseReviewOps.ts`、`qualityOps.ts`、`fullCycleFinalizeOps.ts` |
| T4 | publish 门禁「各声明端均 go」 | P0 | T3 | pending | `application/workspace/project/publishOps.ts` |
| T5 | interfaces schema + 校验 | P1 | T2 | pending | `interfaces/http/routes/workspaceProject*.ts`、`projectService` |
| T6 | 前端端声明 + 按端展示 | P1 | T1 | pending | `v2/src/pages/projects/*` |
| T7 | 测试新增 + 适配 ~15 | P0 | T3,T4 | pending | `v2/backend/tests/*` |
| T8 | 归档质检 + verify:all | P0 | T7 | pending | 本文件 + git |

## 对后续版本的约束

- TargetPlatform 为固定枚举，新增端加枚举值（端集合有限稳定，不引入开放字符串）；
- releaseReview.perPlatform 为发布评审结构的一等字段，后续 v0.30.0 测试矩阵按端分组将复用该端轴；
- publish 门禁「各端均 go」为不可绕过的硬约束（blocker 须标明是哪个端）。
