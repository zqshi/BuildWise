# v0.14.0 Next — 流畅性架构债 + 遗留项清理（规划版）

> 状态: 规划中（待激活，2026-06-26）
> 上一版本: [v0.13.0](v0.13.0-snapshot.md)

## 版本目标（待规划确认）

v0.13.0 完成需求池归属交互 + 三道门禁加固（契约子进程模式跑通、契约接入门禁、Prompt 检测器扫 src/），verify:all 真绿。

本版本聚焦 v0.13.0 流畅性审查暴露的**架构级流畅性短板**（非局部 bug），并清理跨版本遗留。当前方向待用户确认，候选：

1. **fullCycle 异步化**（流畅性最大短板）：fullCycle 全管道（分析+确认+UX+前后端改写+测试+评审+发布）同步阻塞单 HTTP 连接，涉及 5-6 次 LLM + agent 执行，可能 30min+，任何网关超时即断连。复用 v0.8.0 已落地的 `codeRewriteJobOps` 异步 Job 状态机（pending→running→completed/failed/timeout）模式改造。
2. **分析 Job 重启恢复**：analysisJobs 内存态，进程重启后 running 任务变"幽灵 running"无 worker，前端轮询永远等不到。`reconcileAnalysisJobsOp` 补 DB=running 无 worker 恢复逻辑。

## 版本范围

**做什么（待确认）:**
- fullCycle 改异步 Job 化（复用 codeRewriteJobOps 状态机模式）
- 分析 Job 重启恢复"幽灵 running"
- fullCycle 步骤失败文件级补偿（回滚已写盘文件或暂存区提交）
- T2 路径A 完整 dryRun 脚本（v0.12.0/v0.13.0 deferred 迁入）

**不做什么:**
- T4 前端 Props Drilling / JSON 持久化 → 需先方案规划，继续 deferred
- BacklogItem 独立状态机 → P1，可本版本顺带或单独

---

## 任务依赖表（待激活时填充）

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|---------|
| - | fullCycle 异步化 | P0 | - | 待规划 | fullCycleOps.ts / workspaceIterationAnalysisRoutes.ts / 新 jobOps |
| - | 分析 Job 重启恢复 | P1 | - | 待规划 | analysisService.ts / queueOps.ts |
| - | fullCycle 步骤失败文件补偿 | P1 | - | 待规划 | codeRewriteOps.ts / fullCycleOps.ts |
| T2 | 路径A 完整 dryRun 脚本 | P2 | - | deferred | dryrun-code-rewrite.mjs |
| T4 | 技术债务深化 | P3 | - | deferred | v2/src/pages/projects/ |

状态值: pending / in-progress / blocked / deferred / done

## 约束

- 保持 verify:all 真绿（v0.13.0 后含子进程契约 + in-process 契约 + prompt 动态扫描）
- 遵守迭代声明纪律（docs/versions + 任务依赖表 + 6 项质量检测协议）
- 不修改 OpenClaw 源码
- Agent 框架可切换（声明+运行时分离）
- fullCycle 异步化优先复用 codeRewriteJobOps 既有状态机模式，不新造轮子
- fullCycle 步骤失败补偿遵循"不造假"原则（CLAUDE.md）：禁止半改写状态透传
