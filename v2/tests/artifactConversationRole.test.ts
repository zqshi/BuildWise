import test from "node:test";

// TODO: rewrite as behavior test — import publishArtifactReferenceMessage, mock repo, verify message role
test.skip("appendIterationArtifactToConversationOp writes artifact references as assistant messages", () => {});
