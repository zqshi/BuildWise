# 迭代 1：统一模型最小闭环

目标：实体/字段/规则/页面可创建与展示（骨架）。

frontend
backend

PAGE: 模型编辑器 | /model-editor
API: GET /api/entities
API: GET /api/rules
FIELD: Entity.name string required
FIELD: Field.name string required
FIELD: Rule.name string required
