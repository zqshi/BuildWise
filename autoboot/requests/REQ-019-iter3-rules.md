# 迭代 3：规则编译 MVP

目标：自然语言规则 → 结构化规则占位与字段绑定流程骨架。

frontend
backend

PAGE: 规则编译 | /rules
PAGE: 规则绑定 | /rule-binding
API: GET /api/rules/compile
API: GET /api/rules/bind
FIELD: Rule.type string required
FIELD: Rule.target string required
FIELD: Rule.message string
